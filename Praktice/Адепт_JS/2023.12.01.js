//Напишите функцию, которая принимает строку и возвращает true, если эта строка представляет собой число, то есть состоит только из цифр. Иначе возвращает false.

//ts
//const isNum = (str: string) => Boolean(Number(str));
